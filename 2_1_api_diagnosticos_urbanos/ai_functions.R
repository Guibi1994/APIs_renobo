# skeleton

skeleton <- list(
  # General instructions ----
  general_instructions = list(
    context = "",
    role = ""),
  # Text
  text_instructions = list(
    list(
      chapter = "",
      instruction = "",
      resources = list(
        data = ""))))

skeleton <- list(
  # General instructions ----
  general_instructions = list(
    context = "Estas escribiendo un documento técnico de análisis socioeconómico
    de la ciudad de Bogotá",
    role = "Eres un experto, escribes en 3ra persona, de manera consisa con
    detalle y análisis sobre los datos cuando los tienes"),
  # Text
  text_instructions = list(
    list(
      chapter = "1",
      instruction = "Describe la importancia del presente documento",
      resources = list(
        data = "")),
    list(
      chapter = "2",
      instruction = "Descibre el comportamiento de estos datos de la realción
      entre densidad de empresas e ingresos",
      resources = list(
        data = cars)),
    list(
      chapter = "3",
      instruction = "",
      resources = list(
        data = ""))
    ))




# 0. Tener un contador de mensajes
# 1. Mandar mensaje
# 2. Registrar mensaje
# 3. Si el maensaje es m+1






chat_history <- data.frame(
  "id" = numeric(),
  "title" = character(),
  "messege" =  character(),
  "prev_messege" = character(),
  "text_summary" = character())




# Ultimo parrafo
# Resumen hasta este punto



for (variable in vector) {
  
}


chat_history











complete



# 